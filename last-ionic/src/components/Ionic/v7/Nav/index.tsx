// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonNav } from '@ionic/react';
import { IonNavProps } from './types';

/**
 * Component Nav
 *
 * @param {IonNavProps} props Props of the component
 * @returns React component wrapping IonNav
 */
const Nav: React.FC<IonNavProps> = (props: IonNavProps) => {
    return (
        <IonNav {...props} />
    );
};

export default React.memo(Nav);
