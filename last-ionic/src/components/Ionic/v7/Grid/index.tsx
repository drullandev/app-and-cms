// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonGrid } from '@ionic/react';
import { IonGridProps } from './types';

/**
 * Component Grid
 *
 * @param {IonGridProps} props Props of the component
 * @returns React component wrapping IonGrid
 */
const Grid: React.FC<IonGridProps> = (props: IonGridProps) => {
    return (
        <IonGrid {...props} />
    );
};

export default React.memo(Grid);
