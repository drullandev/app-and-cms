// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonMenuToggle } from '@ionic/react';
import { IonMenuToggleProps } from './types';

/**
 * Component MenuToggle
 *
 * @param {IonMenuToggleProps} props Props of the component
 * @returns React component wrapping IonMenuToggle
 */
const MenuToggle: React.FC<IonMenuToggleProps> = (props: IonMenuToggleProps) => {
    return (
        <IonMenuToggle {...props} />
    );
};

export default React.memo(MenuToggle);
