// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonRadio } from '@ionic/react';
import { IonRadioProps } from './types';

/**
 * Component Radio
 *
 * @param {IonRadioProps} props Props of the component
 * @returns React component wrapping IonRadio
 */
const Radio: React.FC<IonRadioProps> = (props: IonRadioProps) => {
    return (
        <IonRadio {...props} />
    );
};

export default React.memo(Radio);
