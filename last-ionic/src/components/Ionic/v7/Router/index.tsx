// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonRouter } from '@ionic/react';
import { IonRouterProps } from './types';

/**
 * Component Router
 *
 * @param {IonRouterProps} props Props of the component
 * @returns React component wrapping IonRouter
 */
const Router: React.FC<IonRouterProps> = (props: IonRouterProps) => {
    return (
        <IonRouter {...props} />
    );
};

export default React.memo(Router);
