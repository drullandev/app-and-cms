// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonTabBar } from '@ionic/react';
import { IonTabBarProps } from './types';

/**
 * Component TabBar
 *
 * @param {IonTabBarProps} props Props of the component
 * @returns React component wrapping IonTabBar
 */
const TabBar: React.FC<IonTabBarProps> = (props: IonTabBarProps) => {
    return (
        <IonTabBar {...props} />
    );
};

export default React.memo(TabBar);
