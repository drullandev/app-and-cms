// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonTitle } from '@ionic/react';
import { IonTitleProps } from './types';

/**
 * Component Title
 *
 * @param {IonTitleProps} props Props of the component
 * @returns React component wrapping IonTitle
 */
const Title: React.FC<IonTitleProps> = (props: IonTitleProps) => {
    return (
        <IonTitle {...props} />
    );
};

export default React.memo(Title);
