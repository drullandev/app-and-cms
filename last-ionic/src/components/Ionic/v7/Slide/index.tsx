// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonSlide } from '@ionic/react';
import { IonSlideProps } from './types';

/**
 * Component Slide
 *
 * @param {IonSlideProps} props Props of the component
 * @returns React component wrapping IonSlide
 */
const Slide: React.FC<IonSlideProps> = (props: IonSlideProps) => {
    return (
        <IonSlide {...props} />
    );
};

export default React.memo(Slide);
