// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonChip } from '@ionic/react';
import { IonChipProps } from './types';

/**
 * Component Chip
 *
 * @param {IonChipProps} props Props of the component
 * @returns React component wrapping IonChip
 */
const Chip: React.FC<IonChipProps> = (props: IonChipProps) => {
    return (
        <IonChip {...props} />
    );
};

export default React.memo(Chip);
