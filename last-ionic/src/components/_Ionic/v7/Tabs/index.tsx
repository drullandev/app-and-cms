// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonTabs } from '@ionic/react';
import { IonTabsProps } from './types';

/**
 * Component Tabs
 *
 * @param {IonTabsProps} props Props of the component
 * @returns React component wrapping IonTabs
 */
const Tabs: React.FC<IonTabsProps> = (props: IonTabsProps) => {
    return (
        <IonTabs {...props} />
    );
};

export default React.memo(Tabs);
