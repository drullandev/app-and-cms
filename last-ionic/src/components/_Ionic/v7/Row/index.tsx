// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonRow } from '@ionic/react';
import { IonRowProps } from './types';

/**
 * Component Row
 *
 * @param {IonRowProps} props Props of the component
 * @returns React component wrapping IonRow
 */
const Row: React.FC<IonRowProps> = (props: IonRowProps) => {
    return (
        <IonRow {...props} />
    );
};

export default React.memo(Row);
