// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonNavLink } from '@ionic/react';
import { IonNavLinkProps } from './types';

/**
 * Component NavLink
 *
 * @param {IonNavLinkProps} props Props of the component
 * @returns React component wrapping IonNavLink
 */
const NavLink: React.FC<IonNavLinkProps> = (props: IonNavLinkProps) => {
    return (
        <IonNavLink {...props} />
    );
};

export default React.memo(NavLink);
