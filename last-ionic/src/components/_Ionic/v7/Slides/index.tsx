// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonSlides } from '@ionic/react';
import { IonSlidesProps } from './types';

/**
 * Component Slides
 *
 * @param {IonSlidesProps} props Props of the component
 * @returns React component wrapping IonSlides
 */
const Slides: React.FC<IonSlidesProps> = (props: IonSlidesProps) => {
    return (
        <IonSlides {...props} />
    );
};

export default React.memo(Slides);
