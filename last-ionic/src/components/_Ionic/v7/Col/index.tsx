// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonCol } from '@ionic/react';
import { IonColProps } from './types';

/**
 * Component Col
 *
 * @param {IonColProps} props Props of the component
 * @returns React component wrapping IonCol
 */
const Col: React.FC<IonColProps> = (props: IonColProps) => {
    return (
        <IonCol {...props} />
    );
};

export default React.memo(Col);
