// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonReorder } from '@ionic/react';
import { IonReorderProps } from './types';

/**
 * Component Reorder
 *
 * @param {IonReorderProps} props Props of the component
 * @returns React component wrapping IonReorder
 */
const Reorder: React.FC<IonReorderProps> = (props: IonReorderProps) => {
    return (
        <IonReorder {...props} />
    );
};

export default React.memo(Reorder);
