// This file has been automatically generated
// by the component generation script.
import React from 'react';
import { IonTabButton } from '@ionic/react';
import { IonTabButtonProps } from './types';

/**
 * Component TabButton
 *
 * @param {IonTabButtonProps} props Props of the component
 * @returns React component wrapping IonTabButton
 */
const TabButton: React.FC<IonTabButtonProps> = (props: IonTabButtonProps) => {
    return (
        <IonTabButton {...props} />
    );
};

export default React.memo(TabButton);
